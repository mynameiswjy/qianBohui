<block>
    <view style="height: 10rpx;"></view>
    <!--<view
            class="cell_item"
            wx:for="{{list}}"
            data-theme-name="{{item.name}}"
            wx:key="index"
            data-id="{{item.id}}"
            bindtap="navDetails"
    >
        <image src="{{item.imagesUrl}}" class="bg" mode="widthFix"></image>
        <view class="flex cell_title">
            <image src="./cell.png" class="icon_cell"></image>
            <view class="cell_title_txt">{{item.name}}</view>
        </view>
        <view class="cell_txt">
            <text>{{item.imagesRemark}}</text>
        </view>
    </view>-->

    <view
            class="gold_item_wrap common_wrap"
            wx:if="{{list.length}}"
            wx:for="{{list}}"
            wx:key="index"
            data-theme-name="{{item.name}}"
            data-id="{{item.id}}"
            bindtap="navDetails"
    >
        <view class="gold_item common_content">
            <image mode="" class="img" src="{{item.imagesUrl}}"></image>
            <view class="list-right">
                <view class="list-right-top1">{{item.name}}</view>
                <view class="list-right-bottom">{{item.imagesRemark}}</view>
            </view>
        </view>
    </view>
</block>