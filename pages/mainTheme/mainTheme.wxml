<block>
    <!--<view
            class="sub_item"
            wx:for="{{list}}"
            wx:key="{{index}}"
            style="background-image: url({{item.imagesUrl}});"
            data-is-son="{{item.sonSubject == 'Y'}}"
            data-theme-name="{{item.name}}"
            data-id="{{item.id}}"
            bindtap="navDetails"
    >
        <view class="item_txt">{{item.name}}</view>
    </view>-->
    <swiper class="swiper" previous-margin="70rpx" next-margin="70rpx">
        <swiper-item wx:for="{{5}}" wx:key="index">
            <view class="banner_item"></view>
        </swiper-item>
    </swiper>
</block>